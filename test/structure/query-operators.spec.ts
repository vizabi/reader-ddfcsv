import * as chai from 'chai';
const expect = chai.expect;

xdescribe('Query operators structure errors in query', () => {
  it('should be processed correctly', () => {
    expect(true).to.be.ok;
  });
});
